div.post
  h1.header
    a(href=post.url)
      = post.title
  span.date #{post.date}
  div.post.content
    != post.preview
  div.tags
    - each tag in post.tags
      a(href=tagUrl(tag))
        span.label.label-info #{tag}
